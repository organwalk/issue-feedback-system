server:
  port: 8081
spring:
  application:
    name: issue-feedback-system
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/issue_feedback?useSSL=false
    username: root
    password: 123456

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

allow-api-list:
  # 此处填写允许角色访问的API
  # 请填写实际API
  student:
    - /api/student
    - /api/book/read
  teacher:
    - /api/teacher
  dept-leader:
    - /api/dept
  school-leader:
    - /api/school
  admin:
    - /api/system
    - /api/pending-users